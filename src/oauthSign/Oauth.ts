//handling signup with oauth options

import { createClient } from "@/app/utils/supabase/client";

//handle for google
export async function handleGoogleSignup(){
    const supabase = await createClient()
    try {
        
        
    } catch (error) {
        
    }
}